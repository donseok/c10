"""Rebuild three source-grounded, self-contained Korean design guides."""
from pathlib import Path
import html, subprocess
A=Path(__file__).resolve().parent
B=A.parent
css=(B/'width-design-guide-assets/guide.css').read_text().replace('C10 · 적정·차선 원자재 폭설계','C10 · 성분·재질·인수도 설계')
css+='\n.spec-explorer{padding:24px;background:#eef7f8;border:1px solid #b7c8d5;border-radius:12px}.spec-explorer select{display:block;width:100%;padding:12px;font:inherit;margin:12px 0;color:#17324d;background:white}.spec-explorer output{display:block;font-size:23px;font-weight:700}.spec-explorer .why{margin-bottom:0}@media print{.spec-explorer{display:none}}\n'
(A/'guide.css').write_text(css)
def txt(x,y,s,size=24,color='#17324d'):
 return f'<text x="{x}" y="{y}" font-size="{size}" fill="{color}">{html.escape(str(s))}</text>'
def rect(x,y,w,h,c='#eef7f8'):
 return f'<rect x="{x}" y="{y}" width="{w}" height="{h}" rx="10" fill="{c}" stroke="#b7c8d5"/>'
def svg(name,title,rows,foot='설명용 도식 · 숫자는 운영 기준값이 아닙니다.'):
 h=150+len(rows)*113
 p=txt(30,46,title,30)
 for i,(label,detail) in enumerate(rows):
  y=80+i*113
  p+=rect(30,y,1040,88,'#eef7f8' if i==len(rows)-1 else '#f2f5f8')+txt(50,y+34,label,25)+txt(50,y+67,detail,21)
  if i<len(rows)-1:p+=txt(535,y+111,'↓',24,'#007f86')
 p+=txt(30,h-18,foot,19,'#516579')
 (A/name).write_text(f'<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1100 {h}" role="img"><title>{html.escape(title)}</title><desc>{html.escape(" / ".join(a+": "+b for a,b in rows))}</desc><rect width="1100" height="{h}" fill="white"/><g font-family="Apple SD Gothic Neo, sans-serif">{p}</g></svg>')
def ranges(name,title,rows,lo,hi,unit):
 p=txt(30,45,title,30)+txt(30,80,'가로 막대는 허용되는 범위입니다. 마지막 행에서 함께 허용되는 구간을 읽습니다.',21)
 for i,(label,l,u) in enumerate(rows):
  y=120+i*94;x=lambda v:280+(v-lo)/(hi-lo)*710
  p+=txt(30,y+30,label,24)+f'<line x1="280" y1="{y+20}" x2="990" y2="{y+20}" stroke="#d6e0e8"/>'
  p+=rect(x(l),y,max(2,x(u)-x(l)),36,'#a4dbd4' if i==len(rows)-1 else '#d8e6ee')
  p+=txt(280,y+65,f'{l:g} ~ {u:g} {unit}',21)
 h=160+len(rows)*94;p+=txt(30,h-12,'가상 예제 · 0 또는 빈값이 있는 경우에는 별도 처리 규칙을 확인하세요.',20)
 (A/name).write_text(f'<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1100 {h}" role="img"><title>{title}</title><rect width="1100" height="{h}" fill="white"/><g font-family="Apple SD Gothic Neo, sans-serif">{p}</g></svg>')
def fig(name,caption):return f'\n![{caption}](quality-spec-guide-assets/{name})\n'
def src(path,label):return f'[{label}](../../src/{path})'
common='''
# 세 설계는 어디에 쓰이나요?

성분설계는 **무엇이 얼마나 들어 있는 강재를 허용할지**, 재질설계는 **어떤 강도·연신율·시험조건을 요구할지**, 인수도설계는 **납품 제품의 치수와 형상을 어디까지 허용할지**를 정합니다. 여기서 설계값은 검사 실측값이 아니라, 주문별로 편성하는 요구 사양입니다.

| 구분 | 쉽게 말하면 | 대표 결과 | 저장 테이블 끝 이름 |
|---|---|---|---|
| 성분 | 성분별 허용 함량 | C·Si·Mn 등의 하한/상한 | CHM |
| 재질 | 성능과 시험 요구 | TS·YP·연신율·시험 지시 | MQL |
| 인수도 | 납품 치수·형상 허용 | 두께·폭·길이 공차, 형상값 | DLV |

`QLT_DSN_SPC_TP`는 **1 고객 / 2 규격 / 3 사내 / 4 보증**입니다. 폭설계에서 보았던 적정·차선 구분 `QLT_DSN_MNF_TP`와 별개입니다. 이 세 사양은 주문번호·주문행·사양구분으로 저장하며, 적정·차선 원자재마다 같은 방식으로 별도 행을 만드는 구조가 아닙니다.

메인 서비스의 실행 순서는 **설계 Key → 규격 → 고객 → 사내 → 보증 성분 → 보증 재질 → 보증 인수도 → 제조·원자재·치수 설계 → 타당성 검사**입니다. 사양구분 숫자 순서와 실행 순서를 혼동하지 마세요. 사내 단계는 성분·재질을 다루며 인수도에는 이 클래스의 사내 분기가 없습니다.

> 이 문서는 현재 저장소의 Java·서비스 XML·SQL을 분석했습니다. 예제 숫자와 문자 코드 A/B/C는 설명용 가상값입니다. 운영 MD(기준정보)의 실제 행, 계산식 실행 결과, DB 저장 결과를 조회한 문서는 아닙니다.
'''
svg('common-flow.svg','주문 한 건에서 세 종류의 보증사양이 만들어지는 위치',[
 ('① 주문과 설계 Key','규격약호·규격연도·고객사양번호·품명·재질코드·환산치수 준비'),
 ('② 사양별 기준 조회','규격 2 → 고객 1 → 사내 3 (사내는 성분·재질)'),
 ('③ 주문별 보증사양 4 생성','성분: 범위 비교 / 재질: 범위 + 지시 / 인수도: 공차별 선택'),
 ('④ 제조·치수 설계 및 후속 검사','인수도 공차는 치수설계에 연결 / 재질은 후속 수정 경로도 확인')], '근거: C102100000-service.xml · 숫자 1~4는 사양구분입니다.')
footer='''
# 실제 주문을 확인하는 순서

1. 주문번호와 주문행으로 대상 한 건을 고정합니다.
2. 공통정보에서 규격약호·연도·고객사양번호·품명·재질코드·주문환산치수를 확인합니다.
3. 아래 기준정보의 조회 키와 일치하는 MD 행을 확인합니다. 실제 임계값은 이 MD에 있습니다.
4. 결과 테이블의 사양구분 1·2·3·4를 나란히 봅니다. 인수도에는 이 경로의 3번 사양이 없습니다.
5. 한 항목씩 보증값의 출처를 추적합니다. 빈값과 0을 구별하고, 재질은 후속 수정 여부도 확인합니다.
6. 설계 오류 로그와 상태, 최종 저장값을 함께 봅니다. 이 문서의 계산 예제가 실제 서비스의 정상 완료를 보장하지는 않습니다.

# 함께 읽기

- [성분설계 분석서](성분설계_쉽게이해하기.html)
- [재질설계 분석서](재질설계_쉽게이해하기.html)
- [인수도설계 분석서](인수도설계_쉽게이해하기.html)
- [적정·차선 원자재 폭설계 분석서](적정_차선_원자재_폭설계_쉽게이해하기.html)
'''
# Chemical diagrams
svg('chem-input.svg','탄소 C의 보증범위를 만들기까지',[
 ('규격 조회','규격약호 + 규격연도 + 주문환산두께 → C10B1011'),
 ('고객 조회','고객사양번호 → VI_M00_C10A1021 (고객 성분행은 없을 수 있음)'),
 ('사내 조회','품명 + 재질코드 + 주문환산두께 → C10B1031'),
 ('보증 편성','고객값에서 시작 → 규격 비교 → 사내값이 있으면 추가 비교')])
ranges('chem-range.svg','탄소 C: 하한은 올라가고 상한은 내려갑니다', [('규격',.02,.12),('고객',.03,.10),('사내',.04,.09),('보증',.04,.09)],0,.14,'(함량 예시)')
svg('chem-zero.svg','빈값과 0은 똑같은 숫자 경계로 취급되지 않습니다',[
 ('빈값과 숫자 비교','numCompare(null, 0.10, false) → 0.10'),
 ('0과 숫자 비교','numCompare(0, 0.10, false) → 0.10 (최솟값 0이 아님)'),
 ('둘 다 0 또는 한쪽만 0','0과 0은 0 / null과 0은 0 → 모든 0을 null로 바꾸는 함수도 아님')])
svg('chem-conflict.svg','범위를 좁혔는데 겹치는 부분이 없으면?',[
 ('고객 요구','탄소 하한 0.12 / 상한 0.18'),
 ('규격 요구','탄소 하한 0.02 / 상한 0.10'),
 ('편성 결과','하한 0.12 / 상한 0.10 → 하한이 상한보다 큼'),
 ('해석','가능한 함량이 없음. 편성 클래스는 값을 정렬하거나 자동 타협하지 않음')])
svg('chem-elements.svg','원소마다 독립적인 두 개의 경계를 만듭니다',[
 ('C: 탄소','C_LLV ← 가장 큰 유효 하한 / C_ULV ← 가장 작은 유효 상한'),
 ('Si: 규소 · Mn: 망간','동일한 비교를 각 원소별로 반복'),
 ('나머지 10개 원소','P·S·Cr·Ni·Cu·Al·Ti·Nb·V·N도 각각 비교'),
 ('결과','13개 원소 × 하한·상한 = 26개 성분값. 원소 간 합산·평균은 없음')])
chem='''
# 그림으로 먼저 이해하기: 성분의 허용범위를 정합니다

예를 들어 고객이 “탄소는 0.03~0.10”, 규격은 “0.02~0.12”, 사내는 “0.04~0.09”를 요구한다고 해봅시다. 세 조건을 함께 만족하는 범위는 **0.04~0.09**입니다. 탄소 투입량이나 배합 비율을 계산하는 단계가 아니라, 성분별 요구 하한·상한을 정하는 단계입니다.
'''+fig('chem-range.svg','탄소의 세 요구 범위와 보증범위. 모든 값은 가상입니다.')+'''
하한은 “이 값 이상이어야 한다”이므로 큰 값을, 상한은 “이 값 이하여야 한다”이므로 작은 값을 고릅니다. 평균을 내지 않습니다. 다만 코드의 0·빈값 처리 때문에 모든 경우를 수학적 교집합으로 설명할 수는 없습니다.

# 입력값은 어떻게 기준을 찾아가나요?
'''+fig('chem-input.svg','조회 키가 기준정보를 선택하고, 조회 결과가 주문별 성분사양으로 저장됩니다.')+'''
| 사양 | 조회처 | 입력 키 | 결과 처리 |
|---|---|---|---|
| 고객 1 | VI_M00_C10A1021 | CUS_BTH_PAP_NO | 1행이면 복사, 0행이면 false로 해당 하위 서비스 종료 |
| 규격 2 | C10B1011 | SPC_AVR, SPC_YR, ORD_EXC_THK | 정확히 1개 기준을 요구 |
| 사내 3 | C10B1031 | PRD_NM_CD, MQL_CD, ORD_EXC_THK | 정확히 1개 기준을 요구 |
| 보증 4 | C102100CHM.select | ORD_NO, ORD_LN, 사양구분 | 고객 → 규격 → 사내 순서로 비교 |

고객사양번호가 없으면 바깥 고객 서비스에서 성분·재질 조회를 건너뜁니다. 번호가 있어도 고객 성분 조회 결과가 0행이면 이 성분 하위 서비스는 저장 없이 끝납니다. 보증 편성에서는 **규격행이 반드시 있어야 하며**, 고객행·사내행은 있을 때 반영합니다. 다만 “보증 단계에서 사내행 생략 가능”과 “앞선 사내 기준 조회 실패가 정상”은 서로 다른 이야기입니다.

# 실제 비교를 한 줄씩 따라가기

| 단계 | C 하한 | C 상한 | 선택 이유 |
|---|---:|---:|---|
| 고객으로 시작 | 0.03 | 0.10 | 고객행 값을 작업 문맥에 복사 |
| 규격 0.02~0.12 비교 | 0.03 | 0.10 | max(0.03,0.02), min(0.10,0.12) |
| 사내 0.04~0.09 비교 | 0.04 | 0.09 | 사내 요구가 양쪽을 더 좁힘 |
| 보증행 저장 | 0.04 | 0.09 | 사양구분 4로 INSERT |

숫자가 비어 있지 않고 0도 아닐 때: **보증 하한 = max(고객, 규격, 사내 하한)**, **보증 상한 = min(고객, 규격, 사내 상한)**입니다. 각 항목을 따로 비교하므로 탄소 하한은 사내, 탄소 상한은 고객 등 출처가 달라도 됩니다.
'''+fig('chem-elements.svg','26개 결과값은 같은 방식으로 각 원소별 독립 편성됩니다.')+'''
| 기호 | 원소 | 기호 | 원소 |
|---|---|---|---|
| C | 탄소 | Si | 규소 |
| Mn | 망간 | P | 인 |
| S | 황 | Cr | 크롬 |
| Ni | 니켈 | Cu | 구리 |
| Al | 알루미늄 | Ti | 티타늄 |
| Nb | 니오븀 | V | 바나듐 |
| N | 질소 | LLV / ULV | 하한 / 상한 |

함량 단위와 자릿수는 실제 MD·화면 정의와 함께 확인해야 합니다. 이 클래스는 단위 변환이나 13개 원소 총합 100% 검사를 하지 않습니다.

# 빈값·0·충돌은 어떻게 읽나요?
'''+fig('chem-zero.svg','공통 비교 함수는 양쪽이 숫자일 때 0을 비교 후보에서 제외합니다.')+'''
`numCompare(Object,Object,mode)`는 먼저 빈값을 검사합니다. 두 번째가 비면 첫 번째를, 첫 번째가 비면 두 번째를 반환합니다. 둘 다 있으면 0을 제외한 뒤 큰 값/작은 값을 선택합니다. 따라서 0을 엄격한 상한으로 사용하려는 경우 코드 해석에 주의해야 합니다.
'''+fig('chem-conflict.svg','빈 교집합 사례. 계산값 생성과 유효성 판정은 별도입니다.')+'''
`DbSearchChemData` 자체에는 최종 하한 > 상한을 자동으로 뒤집거나 범위를 완화하는 처리가 없습니다. 뒤의 `DbSearchDsnValidData`가 성분 범위 및 규격·고객과 보증의 관계를 검사하지만, 실제 오류 검출은 검사 구현과 데이터 조건을 함께 확인해야 합니다.

# 예제 선택으로 비교하기

<div id="spec-explorer-slot"></div>

# 코드·저장 구조와 오류 찾기

성분 결과는 `TB_C10_QLT_DSN_CHM`에 저장됩니다. `C102100CHM.select`는 주문번호·주문행·사양구분으로 성분값만 읽습니다. 보증 서비스 `C102100130`은 성분 26개를 초기화하고 편성 클래스를 실행한 뒤 `C102100CHM.insert`로 보증행을 등록합니다.

| 확인 지점 | 대표 코드 | 의미 |
|---|---|---|
| 고객 조회 중복 | KC11 | 고객 성분이 2행 이상 |
| 규격 조회 | KS02 / KS12 | 기준 없음·조회 문제 / 중복 |
| 사내 조회 | KN01 / KN11 | 기준 0건 / 중복 |
| 보증 편성 | KS22 | 규격 성분 결과행 없음 |
| 저장 실패 | TB02 | 보증 성분 INSERT 실패 경로 |

오류코드 이름만으로 원인을 단정하지 마세요. 예를 들어 사내 조회의 필수 입력 누락에는 코드상 KS11이 사용되기도 합니다. 서비스 단계·입력 키·오류 메시지를 같이 확인해야 합니다.
'''
# Mechanical
ranges('mech-range.svg','인장강도 TS: 세 범위로 보증 초깃값을 만듭니다',[('규격',270,410),('고객',280,400),('사내',300,390),('보증 초깃값',300,390)],250,430,'MPa')
svg('mech-test.svg','강재를 당겨 보는 시험에서 무엇을 요구하나요?',[
 ('TS · 인장강도','잡아당기는 하중에 견디는 강도 수준 → 하한·상한 MPa'),
 ('YP · 항복강도','영구 변형이 시작되는 강도 수준 → 하한·상한 MPa'),
 ('ELGN · 연신율','늘어나는 정도의 요구 범위 → 하한·상한'),
 ('시험 지시','굽힘시험 기준·시편 위치·길이·호수도 함께 편성')], '시험 개념도 · 실제 시험 절차와 단위는 해당 기준정보를 확인합니다.')
svg('mech-input.svg','재질코드를 입력으로 삼아 요구 성능을 찾습니다',[
 ('규격 사양 2','규격약호 + 연도 + 환산두께 → C10B1012'),
 ('고객 사양 1','고객사양번호 → VI_M00_C10A1022'),
 ('사내 사양 3','품명 + 재질코드 MQL_CD + 환산두께 → C10B1032'),
 ('보증 사양 4','숫자 범위 8쌍과 시험 지시 4개를 항목별 편성')])
svg('mech-priority.svg','숫자 범위와 시험 지시는 선택 규칙이 다릅니다',[
 ('숫자 범위','TS 등 하한: 큰 유효값 / 상한: 작은 유효값'),
 ('시험 지시: 고객값 A 있음','고객 A · 규격 B · 사내 C → A 유지'),
 ('시험 지시: 고객값 없음','고객 빈값 · 규격 B · 사내 C → B'),
 ('시험 지시: 고객·규격 모두 없음','고객 빈값 · 규격 빈값 · 사내 C → C')])
svg('mech-final.svg','보증 초깃값과 최종 저장값이 달라질 수 있습니다',[
 ('① 보증 편성 직후','TS 고객 280~400 / 규격 270~410 / 사내 300~390 → 300~390'),
 ('② 후속 상태 처리의 해당 분기','고객사양번호가 있으면 C102100MQL.cs_update 호출'),
 ('③ 고객 TS 값 재적용','고객행의 non-null TS 280~400 → 보증행 TS를 280~400으로 수정'),
 ('④ 최종 확인','TS·YP·ELGN·HRB·ER 10개 값에 적용. 시험도금량·지시는 이 SQL 대상 아님')])
mech='''
# 그림으로 먼저 이해하기: 필요한 성능과 시험조건을 정합니다

재질코드가 같은 제품이라도 두께나 고객 요구가 다르면 적용 사양이 달라질 수 있습니다. 이 분석의 중심인 `DbSearchMechData`는 **이미 정해진 재질코드 등을 입력으로 받아 강도·연신율·시험 지시를 편성**합니다. 재질코드 자체의 선정은 앞선 설계 Key 단계와 구별해서 읽습니다.
'''+fig('mech-test.svg','재질사양은 실측 강도가 아니라 시험에서 요구할 조건입니다.')+'''
# 입력에서 재질사양까지
'''+fig('mech-input.svg','규격·고객·사내는 서로 다른 키로 조회합니다.')+'''
| 사양 | 조회처 | 입력 키 |
|---|---|---|
| 고객 1 | VI_M00_C10A1022 | CUS_BTH_PAP_NO |
| 규격 2 | C10B1012 | SPC_AVR, SPC_YR, ORD_EXC_THK |
| 사내 3 | C10B1032 | PRD_NM_CD, MQL_CD, ORD_EXC_THK |
| 보증 4 | C102100MQL.select | ORD_NO, ORD_LN, 사양구분 |

규격·사내 MD 조회는 정확히 1건을 요구합니다. 고객 조회 0건은 false로 해당 하위 서비스가 끝나며, 고객사양번호 자체가 없으면 바깥 서비스에서 성분·재질 조회를 생략합니다. 보증 단계는 고객행을 먼저 복사하고 규격행을 반드시 읽은 뒤, 사내행이 있으면 추가로 비교합니다.

# 어떤 항목을 설계하나요?

| 항목 | 저장 필드 | 편성 방식 |
|---|---|---|
| 인장강도 TS | TS_LLV_MPA / TS_ULV_MPA | 하한 큰 값 / 상한 작은 값 |
| 항복강도 YP | YP_LLV_MPA / YP_ULV_MPA | 동일 |
| 연신율 | ELGN_LLV / ELGN_ULV | 동일 |
| HRB 경도 | HRB_LLV / HRB_ULV | 동일 |
| ER 시험값 | ER_LLV / ER_ULV | 동일 |
| 시험도금량 전면·후면·전체 | TST_GW_FRN / BAK / TOT의 LLV·ULV | 각각 독립 비교 |
| 굽힘시험 기준 | MQL_BND_TST_STD_CD | 고객 → 규격 → 사내의 첫 빈값 아닌 값 |
| 시편 채취 위치 | TST_PIC_GTH_INST_LOC | 동일 |
| 시편 채취 길이 | TST_PIC_GTH_INST_LTH | 동일; 길이여도 max/min 아님 |
| 시편 호수 | TPG_RGS_NO | 동일 |

총 **수치 8쌍(16개) + 시험 지시 4개 = 20개**입니다. ER은 소스·SQL에서 ER로 표기하므로 이 문서에서는 검증되지 않은 풀이나 단위를 붙이지 않습니다. 시험도금량도 제조 중 작업 도금량 목표와 구별해야 합니다. 전면·후면을 더해 전체값을 만드는 것이 아니라, 이 클래스는 세 범위를 각각 비교합니다.

# TS 하나로 따라가는 보증 편성
'''+fig('mech-range.svg','보증 편성 직후 TS 300~390 MPa. 후속 수정 전의 값입니다.')+'''
| 단계 | 하한 MPa | 상한 MPa |
|---|---:|---:|
| 고객값으로 시작 | 280 | 400 |
| 규격 270~410 비교 | 280 | 400 |
| 사내 300~390 비교 | 300 | 390 |

양쪽 숫자가 유효하고 0이 아니면 하한은 max, 상한은 min입니다. 공통 `numCompare`는 빈값을 먼저 처리한 뒤 0을 비교에서 제외하므로, 예를 들어 상한 0과 400의 결과는 400입니다. 양쪽 0은 0으로 남습니다. 하한이 상한보다 커지는 충돌을 이 편성 클래스가 자동 조정하지는 않습니다.

# 시험 지시는 어떻게 고르나요?
'''+fig('mech-priority.svg','수치의 엄격도 비교와 지시값의 우선 적용은 서로 다른 규칙입니다.')+'''
예를 들어 시편 채취 길이가 고객 80, 규격 100, 사내 120이면 결과는 **80**입니다. 숫자처럼 보여도 이 필드는 범위 비교 목록에 없기 때문입니다. 고객이 비고 규격이 100이면 100입니다. 지시값 0도 `isNull`이 아니면 유지될 수 있어 수치 범위의 0 처리와 다릅니다.

# 꼭 확인할 두 번째 단계: 고객값으로 후속 수정
'''+fig('mech-final.svg','재질설계의 최종값을 설명하려면 보증 편성 이후 SQL까지 따라가야 합니다.')+'''
`DbSearchDsnValidData`의 오류가 없는 쪽 상태 처리에는 고객사양번호가 비어 있지 않으면 `UPDATE_MQL`을 호출하는 경로가 있습니다. 상수는 `C102100MQL.cs_update`이며, 이 SQL은 **보증행 4의 TS·YP·ELGN·HRB·ER 상하한 10개를 고객행 1의 값으로 갱신**합니다. 일반적인 수동/자동 상태 처리와 별도의 자동 처리 경로에 호출이 있습니다. 모든 주문에 무조건 실행된다고 볼 수는 없습니다.

고객 열이 null이면 `NVL(고객값, 기존 보증값)`로 기존값을 유지합니다. 고객값이 0이면 null이 아니므로 0을 적용합니다. 이 규칙은 앞의 `numCompare`와 다릅니다. 특히 **고객행 자체가 없으면 스칼라 하위 쿼리 결과가 null**이므로, “행이 없을 때도 NVL이 기존값을 보존한다”고 읽으면 안 됩니다. 실제 데이터와 실행 분기를 확인해야 합니다.

위 예시에서 보증 편성 직후 300~390이더라도 해당 SQL이 실행되면 고객 TS 280~400이 됩니다. 따라서 결과 화면의 TS가 단순 교집합 계산과 다를 때는 후속 수정부터 추적하세요. 이 SQL은 시험도금량 6개와 시험 지시 4개를 수정하지 않습니다.

# 예제 선택으로 비교하기

<div id="spec-explorer-slot"></div>

# 저장·오류·검사에서 확인할 사항

`C102100140`은 20개 재질값을 초기화하고 보증 편성을 실행한 뒤 `C102100MQL.insert`로 `TB_C10_QLT_DSN_MQL`에 저장합니다. 그 이후 값이 바뀌는지까지 확인해야 최종 결과 설명이 완성됩니다.

| 확인 지점 | 대표 코드·경로 | 확인 내용 |
|---|---|---|
| 고객 조회 중복 | KC11 | 고객 재질행 2건 이상 |
| 규격 조회 | KS03 | 조회 예외·0건·중복에 같은 코드 사용 |
| 사내 조회 | KN02 / KN12 | 기준 없음 등 / 중복 |
| 보증 편성 | KS23 | 규격 재질 결과행 없음 |
| 보증 저장 | TB03 | INSERT 실패 경로 |
| 최종 갱신 | C102100MQL.cs_update | 고객 10개 수치로 수정 여부 |

후속 검사 소스에는 재질 고객사양을 읽는 지점에서 `SELECT_MQL` 대신 **`SELECT_CHM`을 호출하는 코드가 존재**합니다(1116행). 주석상의 의도와 실행 대상이 다릅니다. 검사에서 모든 고객 재질 불일치가 정상 검출된다고 단정하지 않고, 해당 경로의 실행 로그와 조회 결과를 확인해야 합니다. 본 작업은 이 동작을 문서화했으며 운영 코드는 수정하지 않았습니다.
'''
# Delivery
svg('deli-dimension.svg','주문폭 1,000mm와 폭공차는 다른 값입니다',[
 ('주문 기준폭','1,000mm: 제품의 기준 크기'),
 ('보증 폭공차','하한 −1mm / 상한 +2mm: 기준에서 허용하는 차이'),
 ('제품폭 보증범위','1,000 + (−1) = 999mm / 1,000 + 2 = 1,002mm'),
 ('그 다음 폭설계','제품 목표폭·공정수축·마진을 별도로 반영하여 원자재폭 계산')])
ranges('deli-range.svg','같은 주문폭에도 공차가 바뀌면 허용범위가 달라집니다',[('규격 예시',998,1003),('고객 예시',999,1002),('E/C/D 보증',999,1002)],997,1004,'mm')
svg('deli-input.svg','규격 인수도 사양은 두 번 조회합니다',[
 ('① 규격 공통 조회','규격약호 + 연도 → VI_M00_C10A1010 → 인수도규격 ACPT_RT_SPC'),
 ('② 인수도 기준 조회','인수도규격·품명·제품형태·Edge·환산두께·환산폭·환산길이 → C10B1013'),
 ('③ 규격 사양 저장','치수공차 6개 + 형상값 8개 → DLV 사양구분 2'),
 ('④ 고객과 보증 편성','고객 인수도 경로 결과를 DLV 1에 저장한 뒤 규격과 비교하여 DLV 4 생성')])
svg('deli-customer.svg','고객 공차: 주문값·고객 MD·관리코드의 실제 적용 순서',[
 ('출발','주문에 입력된 두께·폭·길이 공차를 복사'),
 ('고객 인수도 MD 행이 있으면','그 행으로 덮어씀. 로컬 관리코드 3개를 빈값으로 강제 설정'),
 ('고객 MD 행이 없으면','각 관리코드가 빈값/Z이면 유지, 그 외 코드이면 치수별 기준 조회'),
 ('치수별 조회','두께: C10B1013 + C10B2180 / 폭: C10A2181 / 길이: C10A2182')])
svg('deli-pair.svg','두께·길이와 일반 품명의 폭: 상하한을 한 쌍으로 판단',[
 ('고객 양쪽 모두 빈값','고객 [빈값, 빈값] / 규격 [−2, +3] → [−2, +3]'),
 ('고객 한쪽만 있음','고객 [빈값, +2] / 규격 [−2, +3] → [빈값, +2]'),
 ('고객 양쪽 있음','고객 [−3, +4] / 규격 [−2, +3] → [−3, +4]'),
 ('읽는 방법','한쪽만 비어 있어도 규격값으로 채우지 않음. 항상 엄격한 쪽을 고르는 규칙이 아님')])
svg('deli-special.svg','품명 E·C·D의 폭공차에는 별도 비교가 있습니다',[
 ('하한','numCompare(고객 하한, 규격 하한, true): 큰 유효값, 0 제외'),
 ('상한: 양쪽 모두 값 있음','Math.min(고객 상한, 규격 상한): 작은 값, 여기서는 0도 포함'),
 ('상한: 한쪽이 빈값','numCompare(..., true)로 남아 있는 값 적용'),
 ('예제','고객 [0, 0] / 규격 [−2, +3] → 보증 [−2, 0]')])
svg('deli-shape.svg','형상값은 치수공차와 별도로 항목마다 선택합니다',[
 ('파형·굽음 관련','반곡 H · 중곡 H · 외곡 H → 고객값이 있으면 유지'),
 ('직선·각도 관련','직선도 상한 · 직각도 상한 · 대각선차 상한 → 고객값이 있으면 유지'),
 ('기타 형상','급준도 · Telescope → 고객값이 있으면 유지'),
 ('고객값이 빈 항목','해당 항목만 규격값으로 보충. 더 작은 값 비교가 아님')], '명칭은 C102100DLV SQL 주석 기준. 측정 방향·길이·단위는 MD 정의를 확인합니다.')
deli='''
# 그림으로 먼저 이해하기: 납품 제품의 허용 오차를 정합니다

이 프로젝트의 인수도설계는 납품 치수공차와 형상 사양을 편성합니다. 먼저 **기준 치수**와 **공차**를 구별하면 이해가 쉽습니다. 주문폭 1,000mm에 공차 −1~+2mm라면 제품폭의 보증범위는 999~1,002mm입니다. 원자재폭 999~1,002mm를 주문한다는 뜻은 아닙니다.
'''+fig('deli-dimension.svg','제품의 허용범위를 먼저 정하고, 제조 목표·원자재폭은 후속 치수설계에서 계산합니다.')+fig('deli-range.svg','설명용 주문폭 1,000mm, 고객 공차 −1~+2, 규격 공차 −2~+3의 사례.')+'''
# 규격 공차는 어디서 찾나요?
'''+fig('deli-input.svg','규격약호가 곧바로 공차 숫자가 되는 것이 아니라 인수도규격을 거쳐 기준을 조회합니다.')+'''
| 단계 | 조회처 | 입력 |
|---|---|---|
| 규격 공통 | VI_M00_C10A1010 | SPC_AVR, SPC_YR |
| 규격 인수도 | C10B1013 | ACPT_RT_SPC, PRD_NM_CD, PRD_SHP, ORD_EDG_ASG_TP, ORD_EXC_THK, ORD_EXC_WTH, ORD_EXC_LTH |
| 고객 인수도 | VI_M00_C10A1023 | CUS_BTH_PAP_NO |
| 폭 관리코드 | VI_M00_C10A2181 | ORD_WTH_MNG_CD |
| 길이 관리코드 | VI_M00_C10A2182 | ORD_LTH_MNG_CD |
| 두께 관리코드 계산 | C10B2180 | 규격 두께공차 하한·상한, ORD_THK_MNG_CD, L/U 구분 |

`C10B2180`은 외부 MD 계산 호출입니다. 이 Java에는 관리코드별 계산식 본문이 없으므로, 특정 관리코드의 수치를 임의로 공식화하지 않았습니다. 실제 식과 MD 입력·출력을 확인해야 합니다.

# 고객 공차가 만들어지는 순서
'''+fig('deli-customer.svg','고객 MD가 존재할 때의 강제 초기화가 관리코드 적용 여부를 바꿉니다.')+'''
1. 주문 공차 `ORD_THK_TLN_*`, `ORD_WTH_TLN_*`, `ORD_LTH_TLN_*`에 값이 있으면 작업값에 복사합니다.
2. 고객사양번호가 있으면 고객 인수도 뷰를 조회합니다. **한 행 이상 있으면 첫 행을 읽고 로컬 두께·폭·길이 관리코드를 모두 빈값으로 바꿉니다.** 이후 고객 MD의 공차를 복사하므로 기존 주문값을 null로 덮어쓸 수도 있습니다.
3. 관리코드가 모두 빈값 또는 Z이면 성공으로 종료합니다. 고객 MD 행을 읽은 경로는 위 강제 초기화로 이 조건을 만족합니다.
4. 고객 MD 행이 없고 관리코드가 빈값/Z가 아니라면 치수별로 기준을 조회해 공차를 다시 정합니다. 두께는 규격 공차에 관리코드를 적용하는 MD 계산을 두 번 호출하고, 폭·길이는 관리코드 뷰의 값을 복사합니다.

따라서 “주문값이 무조건 최우선” 또는 “관리코드가 있으면 언제나 고객 MD보다 우선”으로 설명하면 현재 코드와 맞지 않습니다. 바깥 고객 서비스는 고객사양번호가 없어도 인수도 하위 서비스로 진입할 수 있어 주문 공차·관리코드 경로가 가능합니다.

# 보증사양 선택 규칙을 비교해 봅시다

| 항목 | 고객값 적용 | 고객값이 비었을 때 | 규격과 엄격도 비교 |
|---|---|---|---|
| 두께 공차 한 쌍 | 한쪽이라도 있으면 쌍 유지 | 양쪽 모두 비었을 때만 규격 쌍 적용 | 하지 않음 |
| 길이 공차 한 쌍 | 동일 | 동일 | 하지 않음 |
| E/C/D 외 품명의 폭공차 | 동일 | 동일 | 하지 않음 |
| E/C/D 폭 하한 | 규격과 numCompare(true) | 남아 있는 값 | 큰 유효값, 0 제외 |
| E/C/D 폭 상한 | 양쪽 값이 있으면 Math.min | 한쪽 빈값이면 남아 있는 값 | 작은 값, 0 포함 |
| 형상 8개 항목 | 항목별 고객값 유지 | 해당 항목만 규격 적용 | 하지 않음 |
'''+fig('deli-pair.svg','두께·길이·일반 폭은 한 쌍을 유지합니다. 고객 한쪽 빈값을 자동 보충하지 않습니다.')+'''
여기서 “일반 품명”은 단지 이 분기에서 E·C·D를 제외한 품명을 뜻합니다. 제품 분류를 새로 정의한 용어가 아닙니다. **0은 빈값이 아니므로 [0,0]도 고객 쌍이 있는 것으로 간주**됩니다.
'''+fig('deli-special.svg','2024-06-13 변경 주석이 있는 E·C·D 폭공차 분기. 실제 조건은 품명코드 비교입니다.')+'''
예를 들어 고객 −1~+2, 규격 −2~+3이면 E/C/D 보증은 −1~+2입니다. 하지만 고객 하한 0과 규격 하한 −2를 비교하면 공통 함수가 0을 제외하여 **−2**를 선택합니다. 상한에서 고객 0과 규격 +3을 비교하면 이 분기의 직접 `Math.min`은 **0**을 선택합니다. 같은 공차 쌍에서도 0을 다르게 다룹니다.

# 형상은 어떤 항목을 설계하나요?
'''+fig('deli-shape.svg','형상 8개 값은 고객 우선, 빈값이면 규격으로 보충합니다.')+'''
| 분류 | 필드 | 의미 |
|---|---|---|
| 두께 | THK_TLN_LLV / ULV | 두께 공차 하한·상한 |
| 폭 | WTH_TLN_LLV / ULV | 폭 공차 하한·상한 |
| 길이 | LTH_TLN_LLV / ULV | 길이 공차 하한·상한 |
| 굽음·파형 | HWAV_H / MWAV_H / EWAV_H | 반곡 H / 중곡 H / 외곡 H |
| 직선·각도 | SLR_ULV / RAR_ULV / DGLN_DIF_ULV | 직선도 / 직각도 / 대각선차 상한 |
| 기타 | STPN / TLC | 급준도 / Telescope |

형상값을 모두 mm로 간주하거나 모든 상한에서 작은 값을 고르면 코드와 달라집니다. 예를 들어 고객 직선도 상한 5, 규격 3이면 이 편성 단계에서는 고객 **5**를 유지합니다. 측정 기준길이·단위와 허용 여부는 기준정보 및 후속 검사를 함께 확인합니다.

# 예제 선택으로 비교하기

<div id="spec-explorer-slot"></div>

# 폭·두께 설계와 연결해서 보기

`DbSearchWthSizeData`는 `WTH_TLN_LLV/ULV`, `DbSearchThkSizeData`는 `THK_TLN_LLV/ULV`를 읽습니다. 인수도설계는 제품의 허용범위를 제공하고, 후속 치수설계는 목표치와 공정별 여유·수축 등을 적용합니다. 같은 주문폭이라도 공차와 목표폭은 서로 다른 역할을 하므로 “보증 폭공차 상한 = 원자재 폭여유”로 바꾸어 읽지 마세요.

# 저장·오류·코드상 주의점

인수도 결과는 `TB_C10_QLT_DSN_DLV`에 저장합니다. 보증 서비스 `C102100150`은 14개 값을 초기화하고 보증 편성 후 `C102100DLV.insert`를 실행합니다. 이 클래스의 보증은 고객 1과 규격 2만 읽으며 사내 3을 읽는 분기가 없습니다.

| 확인 지점 | 대표 코드 | 의미 |
|---|---|---|
| 규격 공통 조회 | KS01 / KS11 | 0건 / 중복 |
| 규격 인수도 기준 | KS30 / KS21 | 조회 실패·0건 / 중복 |
| 두께 관리코드 계산 | KS05 | C10B2180 계산 예외 |
| 폭 관리코드 조회 | KS09 / KS19 | 0건 / 중복 |
| 보증 편성 | KS24 | 규격 인수도 결과행 없음 |
| 보증 저장 | TB04 | INSERT 실패 경로 |

고객 인수도 조회는 `count() != 0` 분기가 `count() > 1`보다 먼저 있어 **중복도 첫 행 사용 경로에 들어갑니다**. 뒤에 적힌 중복 오류 KS18이 이 조건에서 실행된다고 설명할 수 없습니다. 첫 행 선택이 운영상 올바른지는 별도 확인이 필요합니다.

또한 후속 `DbSearchDsnValidData`의 인수도 고객 조회 지점은 `SELECT_DLV`가 아니라 `SELECT_CHM`을 호출합니다(1416행). 따라서 보증 편성 클래스의 규칙과 검사 단계의 실제 동작을 구분해야 합니다. 문서는 확인한 코드 동작을 설명하며, 이 잠재 문제를 운영 코드에서 수정하지는 않았습니다.
'''
refs={
'chem': [('com/unionsteel/mes/c10/activity/nui/DbSearchChemData.java','성분 조회·보증 편성: 99~491행'),('service/C102100130-service.xml','보증 성분 서비스'),('query/C102100CHM-query.glue_sql','성분 INSERT·SELECT')],
'mech': [('com/unionsteel/mes/c10/activity/nui/DbSearchMechData.java','재질 조회·보증 편성: 106~492행'),('service/C102100140-service.xml','보증 재질 서비스'),('query/C102100MQL-query.glue_sql','재질 저장·조회 및 cs_update: 123~143행')],
'deli': [('com/unionsteel/mes/c10/activity/nui/DbSearchDeliSpec.java','고객: 138~397행 / 규격: 398~521행 / 보증: 522~687행'),('service/C102100150-service.xml','보증 인수도 서비스'),('query/C102100DLV-query.glue_sql','인수도 저장·조회'),('com/unionsteel/mes/c10/activity/nui/DbSearchWthSizeData.java','후속 폭설계의 공차 입력'),('com/unionsteel/mes/c10/activity/nui/DbSearchThkSizeData.java','후속 두께설계의 공차 입력')]}
for key,title,body in [('chem','성분설계',chem),('mech','재질설계',mech),('deli','인수도설계',deli)]:
 ref=refs[key]+[('service/C102100000-service.xml','전체 실행 순서'),('service/C102100030-service.xml','고객사양 분기와 인수도 연결'),('com/unionsteel/mes/c10/activity/common/DbCommonUtil.java','공통 비교: numCompare 219~249행'),('com/unionsteel/mes/c10/activity/nui/DbSearchDsnValidData.java','후속 검사 및 상태 처리; 재질 갱신 2558·2605·2655행')]
 md=f'---\ntitle: "{title} 쉽게 이해하기"\nsubtitle: "주문 입력에서 보증사양·최종 저장값까지 — 그림·예제·코드로 읽는 C10"\nlang: ko\ndate: "분석 기준: 저장소 소스 · 2026-09-09"\n---\n'+common+fig('common-flow.svg','세 설계의 공통 위치와 후속 처리.')+body+footer+'\n# 소스 근거 찾아보기\n\n행 번호는 분석 시점 기준입니다. 파일을 열어 클래스명·조건문·SQL ID로도 찾을 수 있습니다.\n\n'+'\n'.join('- '+src(p,l) for p,l in ref)+'\n'
 stem=title+'_쉽게이해하기';(B/(stem+'.md')).write_text(md)
 subprocess.run(['pandoc',stem+'.md','--standalone','--embed-resources','--css=quality-spec-guide-assets/guide.css','--toc','--toc-depth=1','--metadata','toc-title=읽는 순서','-o',stem+'.html'],cwd=B,check=True)
 import re,json
 out=B/(stem+'.html');s=out.read_text()
 widget='<section class="spec-explorer" aria-label="설명용 예제 비교"><label for="spec-scenario">예제를 선택하면 결과와 이유가 바뀝니다.</label><select id="spec-scenario"></select><output id="spec-result" aria-live="polite"></output><p class="why" id="spec-why"></p><small>가상값으로 구성한 코드 규칙 설명입니다. 운영 MD 조회·실제 서비스 실행 기능은 아닙니다.</small></section>'
 s=re.sub(r'<div id="spec-explorer-slot">\s*</div>',lambda _:widget,s)
 s=s.replace('</body>',f'<script>const guideKind={json.dumps(key)};</script><script>'+(A/'explorer.js').read_text()+'</script></body>')
 out.write_text(s)
print('Built 3 guides, Markdown sources, and 18 SVG diagrams.')
